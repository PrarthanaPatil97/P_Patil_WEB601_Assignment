import { FlowerPipePipe } from './flower-pipe.pipe';

describe('FlowerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FlowerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
